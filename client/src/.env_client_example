# Supabase (used directly by React for auth, storage, and photo uploads)
VITE_SUPABASE_URL=your-project-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Base URL of the .NET backend (differs between local dev and Railway deploy)
VITE_API_BASE_URL=http://localhost:5000